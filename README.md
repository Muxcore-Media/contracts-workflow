# contracts-workflow

Workflow orchestration: WorkflowStep, WorkflowDefinition, WorkflowEngine interface.

This package is a MuxCore module contract. Modules import this to agree on shared types and interfaces. Core does not import this — core has no domain knowledge.
